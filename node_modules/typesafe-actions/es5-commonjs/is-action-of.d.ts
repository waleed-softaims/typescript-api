import { TypeGetter } from './';
export declare function isActionOf<T extends A, A extends {
    type: string;
}>(actionCreator: ((...args: any[]) => T) & TypeGetter<T['type']>): (action: A) => action is T;
