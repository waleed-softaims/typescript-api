import { returntypeof } from '.';
import { getReturnOfExpression } from '.';
var increment = function () { return ({ type: 'INCREMENT' }); };
var returnOfIncrement = getReturnOfExpression(increment);
describe('Type Utils', function () {
    describe('getReturnOfExpression', function () {
        it('should return null value', function () {
            expect(returnOfIncrement).toBe(null);
        });
        it('should be equal with alias returntypeof', function () {
            expect(getReturnOfExpression).toBe(returntypeof);
        });
    });
});
//# sourceMappingURL=type-utils.spec.js.map