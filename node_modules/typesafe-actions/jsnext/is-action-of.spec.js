"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const _1 = require(".");
describe('isActionOf', () => {
    it('should succeed with same action', () => {
        const increment = _1.createAction('INCREMENT');
        const isActionOfIncrement = _1.isActionOf(increment);
        expect(isActionOfIncrement(increment())).toBeTruthy();
    });
    it('should fail with different action', () => {
        const increment = _1.createAction('INCREMENT');
        const add = _1.createAction('ADD');
        const isActionOfAdd = _1.isActionOf(increment);
        expect(isActionOfAdd(add())).toBeFalsy();
    });
    it('should correctly assert type for EmptyAction', () => {
        const increment = _1.createAction('INCREMENT');
        const isActionOfIncrement = _1.isActionOf(increment);
        const action = { type: 'ERROR' };
        if (isActionOfIncrement(action)) {
            const a = action;
        }
    });
    it('should correctly assert type for FluxStandardAction', () => {
        const add = _1.createAction('ADD', (amount) => ({ type: 'ADD', payload: amount }));
        const isActionOfAdd = _1.isActionOf(add);
        const action = { type: 'ERROR' };
        if (isActionOfAdd(action)) {
            const a = action;
        }
    });
});
//# sourceMappingURL=is-action-of.spec.js.map